const prompt = require('prompt-sync')();

let n1 = Number(prompt('Digite o primeiro n°: '));
let n2 = Number(prompt('Digite o primeiro n°: '));
let divisao = n1 / n2;
console.log(`A divisão entre ${n1} e ${n2} é igual à ${divisao}`);

